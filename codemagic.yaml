workflows:
  ios-testflight:
    name: iOS TestFlight
    max_build_duration: 120
    instance_type: mac_mini_m1
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.illuminations.contemplations
      vars:
        BUNDLE_ID: "com.illuminations.contemplations"
        XCODE_WORKSPACE: "ios/WordGenerator.xcworkspace"
        XCODE_SCHEME: "WordGenerator"
      node: 18.17.0
      xcode: latest
      cocoapods: default
    scripts:
      - name: Install npm dependencies
        script: |
          npm install
      - name: Install CocoaPods dependencies
        script: |
          cd ios && pod install
  publishing:
    app_store_connect:
      apple_id: paul.todorov.mail@gmail.com
      password: nhtm-zncf-knem-mkpv
      submit_to_testflight: true
      submit_to_app_store: false
      